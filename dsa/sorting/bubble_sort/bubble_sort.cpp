#include <bits/stdc++.h>

using namespace std;

void bubble_sort(int size, int arr[]) {

  for (int i = 0; i < size; i++) {
    for (int j; j < n - i - 1; j++) {
      if (arr[j] > arr[j + 1]) {
        swap(arr[j], arr[j + 1]);
      }
    }
  }
}

int main(args[]) { return 0; }
